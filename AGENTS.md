# AGENTS.md

## 基础规则

- 默认使用中文沟通和编写项目文档，代码与已有命名保持一致。
- 遵从用户当前明确指令、已确认需求与设计、仓库规则、阶段计划的优先顺序；无法消除冲突时先记录冲突和影响。
- 只处理当前任务明确范围，不覆盖或移动未确认归属的文件，不把假设写成硬件事实。
- 未经明确要求，不构建、烧录、调试、运行板级操作或提交代码改动。

## 上下文恢复

开始设计、实现、验证或评审前：

1. 阅读 `README.md` 和 `PROJECT_CONTEXT.md`。
2. 核对当前工作项、状态、分支、基线和下一步。
3. 阅读 `00_Project/WORKFLOW.md` 与 `current_status.md`。
4. 按 Required Reading 读取当前阶段文档、目标目录 `AGENTS.md` 和相关设计资料。
5. 修改前确认范围、验收条件和允许操作。

仓库文件和 Git 历史是正式上下文来源。未落盘的聊天结论不作为冻结的需求、硬件事实或验证结果。

## 阶段与角色

- Design Role 负责需求澄清、设计和实施计划；未获 Project Owner 批准前不进入施工。
- Implementation Role 只修改当前计划列明的文件和范围。
- Verification Role 对照验收条件记录可回读证据，并区分代码验证与硬件验证。
- Review Role 对照需求、设计、变更和验证证据给出通过、返工或阻塞结论。
- Project Owner 负责确认需求与设计、解决硬件事实冲突、接受硬件验收并关闭阶段。
- 不因任务本身默认拆分 Agent；仅在子任务独立且资源不冲突时并行。

## 工程事实与安全

- 新板引脚、时钟、DMA、IRQ、器件型号和电气行为以已确认的原理图、数据手册或测量证据为准。
- 外部来源工程只提供复用参考；不得将来源板卡事实直接写成本项目事实。
- 本机工具路径保存在本地配置中，不写入源码、阶段设计或已提交配置。
- 构建、调试和测试生成物写入 `06_Output` 或对应测试输出目录；正式验证结论写入 `04_Test/Reports`。
- 报告结论必须区分工具链动作成功、代码验证和真实硬件验证。
