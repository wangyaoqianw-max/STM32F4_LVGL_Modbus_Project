# 项目工具

本目录后续逐步加入项目 PC 侧脚本和自动化入口。当前只保留本说明，不迁移来源工程的 Toolkit、脚本、工具适配器或本机配置。

增加工具时先说明它解决的实际工作、调用入口、输入/输出、机器依赖、共享硬件资源和验证方法。工具数量或责任边界尚未稳定前，不预建 Core/Adapters/Workflows 等抽象层；出现重复能力或明确编排需求后再拆分。

生成的日志和临时结果写入 `06_Output`；阶段验证结论写入 `04_Test/Reports`。本机安装路径不得写入工程脚本或提交配置，后续如需配置文件，应同时提供可提交的示例并忽略本机覆盖文件。
